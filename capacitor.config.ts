import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'interface-netflix',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
